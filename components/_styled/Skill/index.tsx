import { SkillProps } from '@/types/general'

const Skill: React.FC<SkillProps> = ({
  title,
  skillIcon,
}) => {
  const {
    alt,
    width,
    height,
    webpUrl,
  } = skillIcon
  return (
    <></>
  )
}

export default Skill